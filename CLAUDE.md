# CLAUDE.md

这个文件是给 **Claude Code** 看的项目说明与协作规范。
每次进入本仓库,请先读完本文件,再开始工作。

---

## 项目背景

<!-- TODO: 项目想法定下来后补充 -->
这是一个多人协作的暑假项目,目前处于构思阶段。具体主题、技术栈、目录结构会陆续补充。
在动手写代码前,如果发现这一节还是空的,请先向用户确认项目要做什么。

## 技术栈

<!-- TODO: 选定后填写 -->
- 待定

## 目录结构

<!-- TODO: 有代码后补充 -->
- 待定

---

## 协作规范(重要 —— 多人 + 多个 Claude 协作的核心规矩)

本项目由多人协作,每个人各自使用自己的 Claude Code。请所有 Claude 实例严格遵守以下约定,
以保证大家行为一致、不互相冲突。

### 1. 分支
- **不要直接修改 `main` 分支。** `main` 受保护,只能通过 PR 合并。
- 每个任务都新开一条分支,命名格式:`<名字>/<功能简述>`
  - 例:`alice/login-page`、`bob/db-schema`
- 一个分支只做一件事,做完就提 PR,不要把多个不相关的改动堆在一条分支上。

### 2. 任务来源
- 所有任务都来自 **GitHub Issues**。动手前先确认是在处理哪个 Issue。
- 如果用户给的任务还没有对应的 Issue,可以先建一个 Issue 再开始。

### 3. Pull Request
- 完成后提交一个 **draft PR**(草稿 PR)。
- PR 描述里关联对应的 Issue,写明:`Closes #<编号>`(合并后会自动关闭该 Issue)。
- PR 标题简洁说明做了什么。
- 等对方 review 后再合并,合并目标是 `main`。

### 4. 提交信息(commit message)
- 用清晰、描述性的信息说明这次改动做了什么。
- 一次提交聚焦一个逻辑改动。

### 5. 开始前先对齐
- 开始一个任务前,先简要说明你理解的任务内容和打算怎么做,等用户确认后再动手。

---

## 工作准则(所有 Claude 实例都要遵守)

做任何判断、建议、决策时,严格遵守下面三条:

1. **判断要有依据。** 所有判断、建议、结论只能基于两个来源:
   (1)**团队成员的真实输入**(他们说的偏好、约束、背景、决定);
   (2)**联网查证到的客观信息**(用搜索/查资料核实,而不是凭记忆)。
   **不要凭空臆断、脑补或编造**。拿不准的就去查或去问,别假装知道。
2. **先 plan 再动手。** 涉及选型、定方案、写较多代码/文档的改动,先给出清晰的计划,
   等团队确认后再写。不要一上来就闷头改。
3. **信息不足就问。** 遇到信息缺失、需求有歧义、或需要团队偏好时,
   **主动向团队提问**获取更多信息,而不是自己假设着往下做。

---

## 给协作者的快速上手

1. 确认自己是本仓库的协作者(已接受邀请)。
2. 读 `README.md`(项目是什么)和本文件(协作规矩)。
3. 读 `docs/协作工作流.md`(怎么跑这套流程:互相 review、交接格式、目录分线等)。
4. 看 GitHub Issues,认领一个任务。
5. 按上面的规范:开分支 → 实现 → 提 draft PR → 关联 Issue。
